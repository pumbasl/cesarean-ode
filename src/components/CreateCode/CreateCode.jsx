/** @jsx jsx */
import React from 'react';
import { jsx, css } from '@emotion/react'

const styled = css`
    margin-top: 50px;
`;

export default function CreateCode(){
    return(
        <div css={styled}>
            s
        </div>
    );
}